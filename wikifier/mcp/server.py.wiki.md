# server.py

Installed MCP entry (`wikifier-mcp`, `python -m wikifier.mcp.server`). Re-exports `mcp` + `main` from `server_impl.py` (complete tool set). Do not register stub `mcp/tools/intel.py` as the live server.
