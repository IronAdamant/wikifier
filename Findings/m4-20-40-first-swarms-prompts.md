# M4 20-40% Swarms Prompts

**Created**: 2026-06-02  
**Purpose**: Prompts for the M4 20-40% execution segment (Core Compaction & State Durability Foundation).

**IRON RULES**: Identical to the 0-20% prompts (zero new dependencies PRIMARY LENS, "3" sacred with explicit FRESH LAST greps + 0 def logs, no dogfood until M5, allowed targets only, subagent_id 100%, 8-step DF + 9 GPs from m3-03, rich diaries, long-term spectrum first, honest calibration).

**MANDATORY FIRST ACTIONS (in addition to the 0-20% reads)**:
- Read the completed 0-20% handoff package and diaries from the previous swarm in full.
- Re-run your own FRESH LAST "3" hygiene greps and log them.

---

## Phase M4-20-40: Core Compaction & State Durability Foundation Working

**Goal**: Deliver the 40% milestone — advanced, reversible compaction policies prototyped for years-scale operation + initial 50k+ creative state boundedness prototypes with zero new dependencies.

**Recommended Swarm Size**: 6–8 agents.

### Agent Roles for 20-40% Swarm

**M4-B1 – Journal & Compaction Lead (Work Area 1 continuation)**
- Own definition and prototype implementation of production-grade, reversible compaction policies for years-scale append-only operation on 50k+ creative monorepos.
- Ensure policies remain bounded and correct under high churn and concurrent load.
- Pure stdlib implementation.

**M4-B2 – Extreme-Scale State Lead (Work Area 2 continuation)**
- Deliver initial 50k+ creative monorepo state boundedness prototypes (barrels + dyn/cond + ws + symlinks + high churn + partials/"3").
- Prove O(changed) behavior and memory/disk/token bounds.
- Zero new dependencies.

**M4-B3 – Long-Term Lens & DF Enforcer**
- Apply 8-step DF + spectrum matrix (tiny vs 50k+ years-scale) to every design decision in this phase.
- Enforce zero-dep as primary lens.

**M4-B4 – "3" Hygiene & Zero-Dep Auditor**
- Own FRESH LAST "3" greps (0 def) for all writes in this phase.
- Audit every new component for accidental new dependencies.

**M4-B5 – Early Evidence Lead (harness + allowed targets)**
- Run the new compaction/state prototypes through extended harness suites on 25k–50k+ creative generators (long-term stress, concurrent chaos).
- Probes on RecipeLab + designated externals.
- Produce 40% evidence with rich metrics.

**M4-B6 – Documentation & Handoff Coordinator**
- Create agent-builder documentation for the 40% foundation.
- Synthesize into clean 20-40% handoff package (modeled on M3 style) with clear handoff to 40-60% swarm.
- Update checklist and maintain calibration.

**Success Criteria for 20-40%**:
- All 40% DoD items from the M4 checklist achieved.
- Compaction policies and state boundedness prototypes exist with zero new deps and clean "3" hygiene.
- Strong early evidence on allowed targets.
- Clear handoff package for the next phase.

---

**subagent_id=m4-20-40-XXX** in 100% of your output.