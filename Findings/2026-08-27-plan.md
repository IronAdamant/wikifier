# Implementation plan — 4.6.12 follow-up (do not execute until accepted)

**Date:** 2026-08-27  
**Status:** Executed 2026-08-27 (WP0–WP7 on this tree). P2 later items not done.  
**Package on disk:** 4.6.13  
**Inputs:** five 2026-08-26/27 research reports (all Partial)

| Slice | Run | Topic |
|-------|-----|--------|
| 1 | `deep-research-2` | Parsers, CDIA, BREE, resolution |
| 2 | `deep-research-3` | Cache, MapScope, ACS, cycles |
| 3 | `deep-research-4` | Health, Core 6, protocol, locking |
| 4 | `deep-research-5` | MCP, CLI, daemon, init/scope |
| 5 | overflow researcher | Tests, CI/packaging, Findings leftover |

**Constraint:** `pyproject.toml` `dependencies = []`. MCP extra stays optional and under `wikifier/mcp/`. Agent-first; no human-dashboard growth. Public library + CLI + optional MCP contracts must keep succeeding. Protocol I/O changes need a `skills/run.md` bump; additive fields do not.

This wave is **hygiene + scale + parser/map honesty**, not a second modularization big-bang. 4.6.10 already restored CLI/MCP, deleted JS/BREE decoys, and put SQLite on the warm path.

---

## Goals

1. Stop this repo’s health matrix looking Critical because of five 2026-08-13 DELETED ghosts the ghost-finder will never prune.
2. Bound MCP/library waits (finite lock timeout, in-process MCP deadline) so ConsistencyHub-scale trees cannot hang behind `timeout=None`.
3. Make the map see the same suffixes `_parse_file` already dispatches; stop dropping `import a, b` and CJS destructure names.
4. Stop `update-maps` walking the whole tree when the `src/` init template is missing; prefer project-relative monitors.
5. Lock MCP/CLI contracts in CI (wheel + `wikifier[mcp]` + `--help`).
6. Close the record-change / skip-mark-green hole in protocol + API (target `AGENTS.md` is operator follow-up, not this package).

Ship as **4.6.13** only after WP0–WP7 tests pass. Do not upload sitting `dist/wikifier-4.6.10-*`.

---

## Non-goals (do not do)

- Full AST, tree-sitter, extra default CDIA detectors, new language parsers, cargo / classpath / `-I` deep resolve.
- Unscoped linux/llvm maps, prettier ACS averages, Mermaid micro-opts, JSON dual-write default-on.
- Bulk re-wiki of 🟢 or Initial stubs.
- Wiki section-patch API or `wikifier attach` / profiles this cycle (G11/G12 stay deferred).
- Fake 72h soak, DoD2 &lt;30s pack, or M5.3.
- Couple core to the `mcp` extra; grow `index.html` / `diagnostics.html` / `serve.py` UX.
- Finish the mechanical `health_pkg/{io,pending,…}` or `cache/` body-move **in this wave** (P2, WP8+). Named modules already exist as re-exports; moving ~2.5k / ~2.4k lines is a later peel, not a blocker.
- ConsistencyHub product docs (`AGENTS.md`) — separate target change after this package ships.

---

## Work packages (do in order)

Write the failing tests in WP0, then the smallest code change that turns them green. `record_change` after every real edit; wiki that file only; `mark_green`. `update-maps` only if imports/structure change.

### WP0 — Contract tests first

Add or extend tests so the later WPs cannot silently regress. Prefer `tests/test_agent_surface_contracts.py` and focused new modules over editing megatest files.

Must fail before the matching WP, pass after:

| Test | Locks |
|------|--------|
| Five DELETED-audit paths are absent from `file_health.json` **or** `find_ghost_entries` / prune returns them for deletion | WP1 |
| Nested `record_change` → `upsert_entry` succeeds (re-entrant); `file_lock(..., timeout=0.25)` on a second fd raises `LockTimeoutError` | WP2 |
| Live `wikifier.mcp.server.mcp` tools are not the `"Not yet implemented"` registrars; `register_intel_tools` is not imported by `server_impl` | WP3 |
| MCP `validate` does not call `_run_wikifier_command` (or a documented exception) | WP3 |
| `candidates.SOURCE_EXTS` and `PARSEABLE_SOURCE_SUFFIXES` include `.mjs/.cjs/.mts/.cts`; a tmp `.mts` file is collected and `_parse_file` returns JS edges | WP4 |
| `import a, b` yields two Python edges; `const {a, b} = require('./x')` keeps both names | WP4 |
| `import wikifier.parsers.python` does not import `wikifier.parsers.javascript` | WP4 |
| Missing `src/` + missing map dirs does **not** walk project root (`dirty_total` / candidate count stays scoped/empty, not full tree) | WP5 |
| Foreign-FS absolute `monitored_paths` entry is ignored (already partly covered by COBOL-farm test) + docs/init agree on relative | WP5 |

Rewrite `tests/test_health.py` DEADLOCK 8s thread timeout into an assertion of re-entrant success or `LockTimeoutError` (do not leave a test named DEADLOCK that encodes a hang).

Do **not** `@skip` barrel tests. Do **not** skip MCP `main` in CI once WP6 installs the extra (keep skipTest only when extra truly absent on a non-CI path).

### WP1 — Prune DELETED reds + doc identity

**Why:** Bootstrap health_score is Critical solely because five renamed files stay 🔴 DELETED forever.

- Teach prune / `find_ghost_entries` (or a dedicated `record_deletion` sweep) to retire `status=DELETED` rows whose path is not on disk **after** an explicit agent/CLI prune — not on every `check_changes` (DELETED is an audit trail). Preferred: CLI `prune-deleted` / extend `prune-health` with a flag, plus one-shot cleanup of these five keys in this repo’s `file_health.json`.
- Paths: `wikifier/health.py`, `wikifier/parsers/bree.py`, `wikifier/import_cache_impl.py`, `wikifier/mcp/server_backup.py`, `wikifier/parsers/javascript/_parser.py`.
- Drop leftover `health.py.wiki.md` if it only documents the deleted module.
- Delete leftover `__pycache__` for those names if present (`health.cpython-*.pyc`, `import_cache_impl*.pyc`, `parsers/bree.cpython-*.pyc`).
- Retarget `CLAUDE.md` and `skills/run.md` off `health.py` / `parsers/bree.py` as live megamodules. `check-changes` is content-hash, not mtime; `prepare_edit` returns wiki/status/deps, not “stage mtime.”
- Define or drop `follow_barrel_chain` in `wikifier/parsers/bree/__init__.py` `__all__`.
- Fix test-count drift in the same doc pass: static `def test_` count is **158** (README ~125, `Claude.md` ~49). CONTRIBUTING must not mention `Basis-v0.3.md`. ISSUE_TEMPLATE contact must point at `docs/spec.md`.

**Done when:** this repo `health --summary` is no longer Critical because of those five rows; docs name `health_impl` / `health_pkg`, `parsers/bree/`, `cache/`, `mcp/server_impl.py`, `parsers/javascript.py`.

### WP2 — Locks: bound wait, nest `_do_*` on record_change

**Why:** Nested same-process flock is already a refcount no-op. Remaining hang is `file_lock(root)` with `timeout=None` held for a whole `check_changes` scan, while MCP/daemon compete.

- Pass finite `timeout=` from MCP mutators into `api.record_change` / `check_changes` / `mark_green`. Default CLI may stay blocking; MCP must not.
- `record_change` already under lock must call `_do_upsert_entry` / `_do_record_meaningful_edit` like `mark_green` uses `_do_mark_green` — not public `upsert_entry` (second `file_lock`).
- Ghost marking in `check_changes`: same `_do_*` while held.
- Protocol text: optional finite `timeout=` already claimed in `skills/run.md` — make `api.py` actually accept it.
- Do not treat this as closing DoD2 MCP &lt;30s. That stays unmeasured evidence.

**Done when:** tests prove nested Core mutators succeed; contended second fd times out; `api` signatures accept `timeout=`.

### WP3 — MCP: deadline, SoT, stubs

**Why:** Live tools are in-process with no wall-clock cap. 60s only wraps `_run_wikifier_command`. Unused `mcp/tools` stubs still ship `"Not yet implemented"`.

- Add a real deadline around in-process `get_project_status`, `get_files_needing_attention`, and `check_changes` (not only the shell fallback). Prefer returning `{success: false, error, timed_out: true}` over hanging the host.
- Paginate or cap status payloads (file list / attention list) so an ~800-file JS tree is not one MCP blob. CLI remains the scale path (`skills/run.md` CLI-at-scale stays).
- MCP `validate` must call `health_impl.validate_health` in-process (same as CLI), not `_run_wikifier_command('validate')`.
- Delete unused `wikifier/mcp/tools/{intel,status,workflow}.py` **or** stop exporting `register_*` from `tools/__init__.py` and add a test that `server_impl` never imports them. Prefer delete if nothing in-repo imports them.
- Keep `_common.py` without a FastMCP instance.
- Do not register stubs on the live `mcp` object.

**Done when:** live 28-tool server has no stub registrar; validate is in-process; a test can force a timeout path without waiting 6000s.

### WP4 — Parser / map honesty

**Why:** Dispatch already knows extra JS suffixes; collect/health/stream do not. Python and CJS drop names agents then never see.

- Add `.mjs/.cjs/.mts/.cts` to `candidates.SOURCE_EXTS`, `health_impl.PARSEABLE_SOURCE_SUFFIXES`, JS relative/index probes, BREE suffix lists, and `cache/_core.py` streaming parse. Single shared tuple if possible (avoid a sixth copy).
- `python.py`: split commas on the `import` form (`import a, b` → two edges). `from … import` already splits.
- `javascript.py`: `require_pattern` / `_extract_imported_names` must see `const {a, b} = require(...)` so BREE can route bindings.
- Stop `python.py` module-level import of `javascript.py` LDSI helpers. New `wikifier/parsers/_ldsi.py` (or equivalent) shared by both.
- Route `parsers/_edge.py` `make_edge` through `contracts.compute_acs_confidence` **or** stop `_pair_from_parser_edge` filling `resolved` from `module` when `resolved_path` is missing — pick one, with a test that unresolved Java/medium edges are `unresolved_project` / skip, not a fake `low_confidence_internal` flood.

**Done when:** suffix matrix test is green; comma-import and CJS destructure tests green; `import parsers.python` does not load `parsers.javascript`.

### WP5 — Scope: no full-tree fallback; relative monitors

**Why:** Init seeds relative `src/`. If that dir is missing, maps fall back to `[root]` while `check_changes` returns empty. Protocol still tells agents to use absolute paths, which break across OS clones.

- `_resolve_map_walk_roots`: if configured map/monitor dirs are all missing, **do not** fall back to project root. Return empty + a structured warning (`scope.ok: false`, action `fix_scope`). Match the 4.6.10 `check_changes` COBOL-farm guard.
- Protocol + `Claude.md` + this-repo `monitored_paths.txt` comments: project-relative paths are canonical for externals; warn (or ignore) foreign-FS absolute prefixes (`/home/...` on macOS, `/Users/...` on Linux). Keep the existing transplant test.
- Init stays comment-guided relative `src/` (already tested). Do not silently seed `.`.
- Optional small fix in the same WP: `assess_autonomous_readiness` `has_cache` should treat `import_cache.sqlite` as a map, not only `import_cache.json` / `library.md`.

**Done when:** tmp project with `monitored_paths=src/` and no `src/` does not enumerate the whole tree; docs no longer mandate abs paths as the only legal external form.

### WP6 — CI / packaging contract

**Why:** `test_server_has_main` skipTest on default CI; wheel is built after tests and never installed.

In `.github/workflows/ci.yml` (and publish test job if cheap):

1. Keep `pip install -e .` + `unittest discover` on 3.8–3.12.
2. Add a job (or step): `pip install -e '.[mcp]'` then `python -m unittest tests.test_agent_surface_contracts` (or the MCP tests) so `main` is not skipTest.
3. After `python -m build`, `pip install` the built wheel in a clean venv and run `wikifier --help` (and `wikifier-mcp --help` if extra installed in that venv).
4. Do **not** upload `dist/wikifier-4.6.10-*`. Fresh build only at release.

Optional follow: add 3.13 to the matrix (local pyc is 3.14; 4.6.12 exists because 3.8/3.9 PEP 585 failed). Not required to ship 4.6.13.

**Done when:** CI log shows MCP `test_server_has_main` actually ran, and a wheel install printed help.

### WP7 — Protocol + API: mark-green is part of the loop

**Why:** ConsistencyHub 2026-08-26: 18 `record-change`, 0 `mark-green`. Protocol says never skip `record_change` and treats greening as a later optional step. API completes Yellow + pending on its own.

- `skills/run.md`: after wiki refresh, **never skip `mark_green`** on the file you just recorded (symmetric with record-change). Not a protocol major bump if this is still v0.6 agent discipline; add a short “close the loop” subsection. Bump protocol only if tool return shapes change.
- `record_change` return already says “Run mark_green after wiki refresh”. Add `needs_mark_green: true` (additive; agents must tolerate new fields).
- `suggest_next_actions` / `build_structured_actions`: if pending LLM edits exist with no subsequent mark-green, emit a concrete `mark_green` action (not only advisory `wiki_refresh`). Do **not** auto-mark-green from `record_change`.
- Runtime selective-work: keep refusing stub-only bulk wiki (`map_first_ok`). Do not start a re-wiki campaign.
- Target-project `AGENTS.md` (ConsistencyHub etc.) is **out of this package PR**. Call it out in the 4.6.13 changelog as operator follow-up.

**Done when:** bootstrap `actions[]` includes `mark_green` for recorded-not-greened files; protocol text matches; no automatic green.

---

## Later (P2) — not this wave unless a WP above forces a touch

Do these only if already editing the file, or as 4.6.14+:

| ID | Item |
|----|------|
| L1 | Real `health_pkg/{io,core,pending,status,analysis,…}` peel; delete star-import of `health_impl` |
| L2 | Move `_core.py` bodies into named `cache/{graph,cycles,acs,barrel,diagnostics,streaming}` |
| L3 | Dirty persist: no full `load_cache` overlay + no double `build_dependency_graph` before graph-signature compare; ACS 1.2→1.3 / missing `library.md` via `save_meta_key` only |
| L4 | Align shell vs Python `discover_project_root` (outermost `.git` + same marker set); stop `wikifier.sh` load-time `export WIKIFIER_PROJECT_ROOT` + staging mkdir before `init --target` |
| L5 | Gate `daemon_loop` on `ready_for_daemon`; drop or gate `serve.py` `update-maps --full` (1800s) |
| L6 | `import wikifier` must not construct FastMCP even when extra is installed (lazy `mcp` package) |
| L7 | Re-measure DoD2 MCP timings on ConsistencyHub / Babylon after WP3; do not invent numbers |

---

## Verification (when executing)

```bash
python -m unittest discover tests
wikifier check-changes
wikifier health --summary          # must not be Critical solely from DELETED audits
wikifier update-maps               # only if WP4/WP5 changed collect/parse
```

Dogfood: this repo + one JS tree with `.mjs` or barrels if convenient. Do **not** claim ConsistencyHub MCP &lt;30s without a timed pack.

Release: bump `__version__` → 4.6.13, CHANGELOG Keep-a-Changelog, tag, Trusted Publishing. Never twine the stale 4.6.10 wheel in `dist/`.

---

## Suggested PR slices (if splitting)

1. WP0 + WP1 (tests + DELETED prune + docs) — safe, visible health_score fix.
2. WP2 + WP3 (locks + MCP deadline/stubs/validate).
3. WP4 + WP5 (parsers + scope).
4. WP6 + WP7 (CI + protocol close-loop) + version bump.

Each slice: tests green, `record_change` / wiki / `mark_green` on touched sources.

---

## Open questions (do not block writing tests)

1. DELETED-row retirement: new `prune-deleted` command vs extend `prune-health`? Recommend extend `prune-health --deleted-missing`.
2. MCP deadline: 30s vs 60s vs caller-supplied? Recommend 60s default, additive `timeout_s` on those tools, CLI uncapped.
3. Protocol bump: WP7 additive fields → stay v0.6; only bump if a required key appears.

---

## Explicitly not a substitute for

- Re-running the full unittest suite in this planning session (not run).
- Confirming 4.6.12 is on PyPI.
- A 72h soak or M5.3.
- Editing ConsistencyHub.
