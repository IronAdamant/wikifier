# tests/test_gap_closure.py

**Role:** Gap-closure regressions — G4 ACS actionable low-conf, G5 health module vs `wikifier.health` function shadow, G7 ghosts/`record-deletion`, map-first readiness/taxonomy, pending prune hygiene.

**Agent note:** Uses temp projects (`TempProjectTestCase`). Does not cover case-alias ghosts (`Claude.md` vs `CLAUDE.md`) — that is separate product debt.
